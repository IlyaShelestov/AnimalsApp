<!DOCTYPE html>
<html lang="en">

  <%- include('partials/head') %>
  
  <body class="mb-5 pb-3">

    <%- include('partials/header') %>

    <main class="mb-4 mb-lg-1 mb-md-1 mb-sm-1">
      <div class="container">
        <% if (language == "ru") { %>
          <h1 class="text-center mt-2">Генератор Случайных Фотографий Собак</h1>
        <% } else { %>
          <h1 class="text-center mt-2">Random Dog Photos Generator</h1>
        <% } %>
        <form
          class="d-flex flex-column align-items-center"
          method="POST"
          action="/dogs"
        >
        <% if (language == "ru") { %>
          <input
          class="mt-2 border blue-border rounded-pill border-2 text-light fw-bold"
          type="submit"
          id="input-submit"
          value="Найти"
        />
        <% } else { %>
          <input
          class="mt-2 border blue-border rounded-pill border-2 text-light fw-bold"
          type="submit"
          id="input-submit"
          value="Generate"
        />
        <% } %>
        </form>
        <div
          class="block-large border-2 blue-border rounded-2 border d-flex justify-content-between align-items-center flex-column mt-3 p-4"
        >
          <img
            class="mw-100 w-auto border-2 blue-border rounded-2 border"
            height="300px"
            src="<%= image %>"
            alt="Dog Photo"
          />
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
